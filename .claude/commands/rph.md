---
description: Read the latest prompt_history entry, act on it, ask if clarification is needed
---

Read the **most recent** entry in this project's `prompt_history/` folder (the file
with the newest date/time in its name, regardless of `.txt` or `.md` extension).
Treat its contents as my request and act on it.

- If anything about the request is ambiguous or underspecified, **ask me clarifying
  questions before doing the work** rather than guessing.
- Follow the project's "Remembering task history" rules in CLAUDE.md. This entry
  arrived as a pre-written file (Method 1): append your response record (Plan +
  Summary) to that same file, then rename it to add a short description
  (`{YYYY_MM_DD_HH_MM}_{short_description}.md`).
