# Jupyter checkpoints
.ipynb_checkpoints/

# Python cache
__pycache__/
*.pyc

# Atomic-write leftovers (editor / tool temp files)
*.tmp.*

# Packaging metadata
*.egg-info/
*.egg
dist/
build/

# output
output/

# Prompt/task history (local only)
prompt_history/
prompt_rationales/
SOMEDAY.md
FINDINGS.md

# Claude working/cache files (generator scripts, diagnostic images, verbatim
# capture buffer, migration backups) -- see CLAUDE.md's "Working / cache files"
cache/

# Local, machine-specific Claude Code settings (e.g. additionalDirectories)
.claude/settings.local.json

# Scratch / throwaway test artifacts
_scratch_*
